if title
  header.main__header
    .content-header
      .container
        h1#content-title.content-header__title= title

        if datePublished
          .content-header__metadata
            .content-header__date
              - var published = dayjs(datePublished).format('YYYY-MM-DD')
              span.content-header__data
                time.content-header__published(datetime=published)= published

              if dateModified
                - var modified = dayjs(dateModified).format('YYYY-MM-DD')
                hr.content-header__sepalate

                span.content-header__data
                  time.content-header__modified(datetime=modified)= modified
                  |
                  | に更新

.main__body
  .content-body
    .container
      != contents
