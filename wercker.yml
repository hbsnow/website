box: nodesource/trusty

build:
    steps:
        # `npm install` command
        - npm-install

        - script:
            name: install gulp
            code: sudo npm install -g gulp
        - script:
            name: build metalsmith
            code: gulp build

deploy:
    steps:
        - lukevivier/gh-pages:
            token: $GITHUB_TOKEN
            basedir: ./public
            repo: hbsnow/4uing.net