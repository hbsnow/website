box: nodesource/trusty
build:
    steps:
        - npm-install
        - script:
            name: install gulp
            code: sudo npm install -g gulp-cli
        - hgen/gulp:
            tasks: build