box: wercker/nodejs

build:
    steps:
        - npm-install

        - hgen/gulp:
            tasks: build

deploy:
    steps:
        - lukevivier/gh-pages:
            token: $GITHUB_TOKEN
            basedir: ./public
            repo: hbsnow/4uing.net