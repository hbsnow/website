doctype xml
rdf:RDF(
	xml:lang='ja',
	xmlns='http://purl.org/rss/1.0/',
	xmlns:rdf='http://www.w3.org/1999/02/22-rdf-syntax-ns#',
	xmlns:atom='http://www.w3.org/2005/Atom',
	xmlns:dc='http://purl.org/dc/elements/1.1/'
)
	channel(rdf:about=site.url)
		title= site.title
		link= site.url
		description プログラムやウェブ制作についての情報を配信しています。
		items
			rdf:Seq
				each item in collections.posts
					- var path = item.path.replace(/_posts.[0-9]{4}-[0-9]{2}-[0-9]{2}-/, 'blog/')
					rdf:li(rdf:resource=site.url + path)/

	each item in collections.posts
		- var path = item.path.replace(/_posts.[0-9]{4}-[0-9]{2}-[0-9]{2}-/, 'blog/')
		item(rdf:about=site.url + path)
			title= item.title
			link= site.url + path
			if item.description
				description= item.description
